name: hello-world
on: push
jobs:
  my-job:
    runs-on:   self-hosted
    steps:
      - name: my-step
        run: echo "Hello World! 🍏 This job's status is ${{ job.status }}."
      - name: Lista de directorios
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."

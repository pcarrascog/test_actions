name: hello-world
on: 
  push:
    branches:
      - dev
      - 'features/**'
jobs:
  my-job:
    runs-on: self-hosted
    steps:
      - name: my-step
        run: echo "Hello World!"
      - name: Lista de directorios
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."
  otro-job:
    runs-on: self-hosted
    steps:
      - name: my-paso
        run: echo "Mi Paso"
